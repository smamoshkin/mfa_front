# AGENTS.md — WB Analytics Dashboard

## Обзор проекта

**WB Analytics Dashboard** — это фронтенд-приложение для анализа финансовой отчетности селлеров на маркетплейсе Wildberries. Приложение предоставляет возможности управления товарами, просмотра аналитики рентабельности, управления налоговыми ставками и синхронизации данных с API Wildberries.

## Технологический стек

### Основные технологии
- **React 19** — библиотека UI
- **TypeScript ~5.9.3** — типизация
- **Vite 7.2** — сборщик и dev-сервер
- **Tailwind CSS 4.1** — CSS-фреймворк
- **React Router DOM 7** — маршрутизация

### Библиотеки
- **Zustand 5** — управление состоянием (с persist middleware для localStorage)
- **Axios 1.13** — HTTP-клиент с интерцепторами
- **React Hook Form 7** + **Zod 4** — валидация форм
- **Recharts 3** — графики и диаграммы
- **React Hot Toast 2** — уведомления (toast)
- **Lucide React** — иконки
- **date-fns 4** — работа с датами
- **TanStack React Table 8** — таблицы

### Структура путей (tsconfig path aliases)
```
@/*         -> src/*
@api/*      -> src/api/*
@components/* -> src/components/*
@pages/*    -> src/pages/*
@store/*    -> src/store/*
@utils/*    -> src/utils/*
```

> **Важно:** На момент анализа path aliases **не используются** в импортах — все импорты относительные (`../api/...`). Это может быть изменено в будущем.

## Структура проекта

```
src/
├── api/                  # API-клиенты и вызовы к бэкенду
│   ├── axiosClient.ts    # Базовый Axios клиент с интерцепторами
│   ├── authApi.ts        # Аутентификация (login, register, profile)
│   ├── analyticsApi.ts   # Аналитика рентабельности, экспорт
│   ├── productsApi.ts    # CRUD товаров и себестоимости
│   ├── dashboardApi.ts   # МЁРТВЫЙ КОД — Dashboard.tsx удалён, нигде не импортируется
│   ├── taxApi.ts         # Налоговые ставки
│   └── syncApi.ts        # Синхронизация с WB
│
├── components/           # Переиспользуемые UI-компоненты
│   ├── AnalyticsFilters.tsx   # Фильтры аналитики (даты, группировка)
│   ├── ClosePeriodModal.tsx   # Модалка закрытия налогового периода
│   ├── CostModal.tsx          # Модалка себестоимости
│   ├── ImageTooltip.tsx       # Тултип с изображением товара
│   ├── ProductModal.tsx       # Модалка создания/редактирования товара
│   ├── ProductDeleteModal.tsx # Модалка удаления товара
│   ├── SalesChart.tsx         # График продаж
│   ├── ToastNotification.tsx  # Компонент уведомлений
│   ├── WbApiKeyModal.tsx      # Модалка ввода API-ключа WB
│   └── ApiStatus.tsx          # Статус API (закомментирован)
│
├── hooks/                # Кастомные React-хуки
│   ├── index.ts
│   └── useTaxRate.ts
│
├── pages/                # Страницы приложения (маршруты)
│   ├── Login.tsx         # Страница входа/регистрации
│   ├── Products.tsx      # Каталог товаров (CRUD)
│   ├── ProductDetail.tsx # Детальная страница товара
│   ├── Analytics.tsx     # Аналитика рентабельности
│   ├── Taxes.tsx         # Управление налоговыми ставками
│   ├── TenantProfile.tsx # Профиль tenant (пользователя)
│   └── ApiTest.tsx       # Страница тестирования API (без защиты)
│
├── store/                # Zustand stores
│   └── authStore.ts      # Состояние аутентификации (persist в localStorage)
│
├── types/                # TypeScript типы
│   ├── analytics.ts      # Типы аналитики рентабельности
│   ├── api.ts            # Типы продуктов, себестоимости
│   ├── dashboard.ts      # МЁРТВЫЙ КОД — используется только dashboardApi.ts
│   └── tax.ts            # Типы налоговых ставок
│
├── utils/                # Утилиты
│   └── apiHealthCheck.ts # Проверка здоровья API
│
├── App.tsx               # Корневой компонент с маршрутами
├── main.tsx              # Точка входа
└── index.css             # Глобальные стили (Tailwind)
```

## Архитектура

### Аутентификация
- **JWT-токены** хранятся в `localStorage` (ключи: `access_token`, `refresh_token`)
- **authStore** (Zustand + persist) хранит `user`, `token`, `isLoading`, `error`
- **axiosClient** автоматически добавляет `Authorization: Bearer <token>` к каждому запросу
- При **401** — попытка refresh (заглушена), затем редирект на `/login`
- **Маршруты** защищены через компонент `PrivateRoute` (проверяет наличие `token`)
- Публичный маршрут `PublicRoute` — только для неавторизованных (Login)

### API-слой
Все API-вызовы организованы в модули:

| Модуль | Базовый URL | Описание |
|--------|-------------|----------|
| `authApi` | `/auth/*`, `/tenants/*` | Логин, регистрация, профиль |
| `productsApi` | `/products/*`, `/product-costs/*` | CRUD товаров и себестоимости |
| `analyticsApi` | `/analytics/*`, `/supplier-reports/*` | Рентабельность, экспорт |
| `taxApi` | `/tax-rates/*` | Налоговые ставки |
| `syncApi` | `/sync/*` | Фоновые задачи синхронизации |

> **Важно:** `dashboardApi` (`/dashboard/*`, `/sync/wb/`) в таблицу выше не включён — это мёртвый код: страница `Dashboard.tsx`, единственный потребитель модуля, удалена из проекта, роут `/dashboard` тоже отсутствует. Модуль по-прежнему возвращает захардкоженные моковые данные (реальные запросы закомментированы), но это уже не актуально, пока страницы нет. См. "Рекомендации для дальнейшей разработки".

### Типы данных

#### Продукт (`Product`)
```typescript
interface Product {
  id: number;
  tenant_id: number;
  sku: string;
  marketplace_sku: string;
  foto: string;
  barcode: string;
  name: string;
  description: string;
  category: string;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}
```

#### Расширенный продукт с метриками (`ProductWithMetrics`)
Расширяет `Product` полями: `current_cost?`, `last_month_sales?`, `last_month_revenue?`, `profitability?`, `stock?`

#### Аналитика рентабельности (`RentabilityResponse`)
Содержит: `period`, `totals`, `rentability` (метрики), `products`, `summary`

#### Налоговая ставка (`TaxRate`)
Содержит: `id`, `tax_rate`, `start_date`, `end_date`, `created_by`, `created_at`, `updated_at`

## Маршруты приложения

| Путь | Компонент | Доступ | Описание |
|------|-----------|--------|----------|
| `/` | Navigate → `/analytics` | Любой | Редирект |
| `/login` | Login | Публичный | Вход/регистрация |
| `/products` | Products | Защищённый | Каталог товаров |
| `/products/:id` | ProductDetail | Защищённый | Детали товара |
| `/analytics` | Analytics | Защищённый | Аналитика рентабельности |
| `/taxes` | Taxes | Защищённый | Налоговые ставки |
| `/profile` | TenantProfile | Защищённый | Профиль пользователя |
| `/api-test` | ApiTest | Публичный | Тестирование API |
| `*` | 404 | Любой | Страница не найдена |

## Переменные окружения

Файл `.env`:
```
VITE_API_BASE_URL=http://localhost:8000/
VITE_APP_NAME=WB Analytics Dashboard
```

Бэкенд ожидается на `http://localhost:8000/`.

## Команды

```bash
npm run dev       # Запуск dev-сервера (Vite + HMR)
npm run build     # Типизация (tsc) + продакшн-сборка
npm run lint      # ESLint проверка
npm run preview   # Предпросмотр продакшн-сборки
```

## Страница Аналитики (актуальное состояние)

Страница `Analytics.tsx` была существенно переработана:

- **Карточки метрик** разбиты на 2 секции: "Основные показатели" (5 карточек, зелёная тема — выручка, перечислено продавцу, маржа, продано товаров + hero-карточка "Общая рентабельность", залитая зелёным/красным по порогу 0%) и "Расходы" (5 карточек, красная тема — реклама, хранение, логистика, налог, DRR).
- Разделы "Визуализация данных" (`AnalyticsCharts.tsx`) и "Детальная аналитика" **удалены полностью** (компонент `AnalyticsCharts.tsx` тоже удалён из проекта). Единственное, что из них перекочевало — карточка DRR (в "Расходы") и таблица "Топ товары по марже" (переросла в общую таблицу ниже).
- Компонент `AnalyticsTable.tsx` тоже удалён (был отдельной таблицей внизу страницы, дублировал функциональность).
- Осталась одна сводная таблица **"Статистика по товарам"** (прямо в `Analytics.tsx`, не вынесена в отдельный компонент) с колонками: Артикул, Маржа, Маржинальность, Оборачиваемость, Категория (маржа/оборачиваемость/общая). Поддерживает сортировку по клику на заголовок и фильтр по каждой колонке (текстовый для чисел, select A/B/C для категорий).

### ABC-категоризация и оборачиваемость

В `Analytics.tsx` реализована ABC-категоризация товаров (функции `getMarginCategory`, `getTurnoverCategory`, `getAbcCategory`, `withAbcCategories`):

- **Категория по марже** считается честно — по накопительному вкладу в общую маржу (A — первые 80% кумулятивной маржи, B — следующие 15%, C — остальное).
- **Категория по оборачиваемости** считается по реальным данным с бэка. Эндпоинт `/analytics/rentability` теперь возвращает поле `turnover_days` в каждом объекте `products[]` (дни запаса: остаток на складе ÷ среднедневные продажи за выбранный период). Тип `RentabilityProduct` (`src/types/analytics.ts`) содержит обязательное поле `turnover_days: number`. `withAbcCategories` берёт его напрямую (`const turnoverDays = product.turnover_days`), без заглушек. Пороги категорий: `<=0` → C (нет данных/остатка), `<=30` → A, `<=60` → B, `>60` → C.
- **Общая категория** — матрица маржа × оборачиваемость (обе оси сильные — A, обе слабые — C, смешанные — B). Теперь, когда оборачиваемость не захардкожена, общая категория A снова достижима у товаров, сильных по обеим осям.
- В таблице «Статистика по товарам» значение оборачиваемости выводится округлённым до одного знака (`turnoverDays.toFixed(1)`), сортировка и фильтр колонки работают по точному числу.

## Особенности и известные моменты

### Заглушки и моковые данные
1. **`dashboardApi`** и **`types/dashboard.ts`** — мёртвый код. Страница `Dashboard.tsx` и роут `/dashboard` были удалены из проекта ещё в коммите `9eda764` («украшательства»), но модуль с мокнутыми данными остался и нигде не импортируется. Рекомендация по исправлению — в разделе "Рекомендации для дальнейшей разработки" ниже.
2. **`*.tsx.mock`** — файлы с расширением `.mock` не компилируются TypeScript и служат справочным материалом (например `Products.tsx.mock`, `authStore.ts.mock`).
3. **Vite proxy** — конфигурация проксирования в `vite.config.ts` закомментирована.
4. ~~Оборачиваемость товара на странице Аналитики~~ — **больше не заглушка**, с бэка приходит реальное поле `turnover_days` (см. раздел "ABC-категоризация и оборачиваемость" выше).

### Закомментированные участки кода
- В `Products.tsx` закомментированы блоки статистики и некоторые колонки таблицы (например загрузка себестоимости для каждого товара, `Products.tsx:52-67`)
- В `analyticsApi.ts` метод `exportPdf` возвращает ошибку (не реализован)
- В `vite.config.ts` целиком закомментирован блок `server.proxy`
- В `axiosClient.ts` (~строка 61) закомментирован реальный вызов `axios.post('/auth/refresh', ...)` в обработчике 401

### Себестоимость
- Каждый товар может иметь историю изменений себестоимости (`ProductCost`)
- Себестоимость имеет период действия (`start_date`, `end_date`)
- Текущая себестоимость определяется по дате

### Налоговые ставки
- Поддержка периодов действия ставок (бессрочные — если `end_date` пустой)
- Возможность закрытия периода через модальное окно
- Валидация: только одна ставка может быть активна в момент времени

### Синхронизация с WB
- Запускается через POST `/sync/wb/`
- Поддержка фоновых задач (task_id, статус задачи)
- История синхронизаций доступна через API

## Паттерны разработки

### Компоненты страниц
- Каждый файл страницы — default export компонента
- Навигация через `react-router-dom` (`<Link>`, `useNavigate`)
- Загрузка данных через `useEffect` + async/await
- Состояния загрузки и ошибок обрабатываются локально

### API модули
- Каждый API-модуль — объект с методами
- Типы импортируются из `src/types/`
- Ошибки пробрасываются выше (обрабатываются в компонентах)

### Формы
- `react-hook-form` + `zod` для валидации
- Схемы Zod определяются рядом с компонентами форм

### Уведомления
- `react-hot-toast` для toast-уведомлений
- `<ToastNotification />` рендерится в `App.tsx` (глобально)
- Используется как `toast.success()`, `toast.error()`, `toast.loading()`

## Рекомендации для дальнейшей разработки

### Выполнено
- ~~Получить остатки товара со склада WB и передавать реальную оборачиваемость в `/analytics/rentability`~~ — сделано. Бэк отдаёт `turnover_days` в каждом объекте `products[]`, фронт (`withAbcCategories` в `Analytics.tsx`) читает реальное значение вместо заглушки `0`. Подробности — в разделе "ABC-категоризация и оборачиваемость".
- ~~`src/api/mockAnalytics.ts`~~ — удалён (был мёртвым кодом, нигде не импортировался, к тому же не проходил тайпчек: несуществующее поле `total_tax_7p`, отсутствующие `type`-импорты).

### Открыто

1. **Удалить или переосмыслить мёртвый код `dashboardApi.ts` / `types/dashboard.ts`.** Страницы `Dashboard.tsx`, которая их использовала, в проекте больше нет (удалена в `9eda764`), роут `/dashboard` тоже отсутствует. Либо снести оба файла, либо (если Dashboard планируется вернуть) — завести отдельную задачу на восстановление страницы и тогда уже раскомментировать реальные API-вызовы в `dashboardApi`.
2. **Использовать path aliases** (`@api/`, `@components/`, `@pages/` и т.д. — уже настроены в `tsconfig`) для чистоты импортов. Сейчас во всём `src` — ни одного импорта через алиас, только относительные пути (`../api/...`).
3. **Реализовать refresh токена** в интерцепторе `axiosClient.ts` (~строка 61) — сейчас при 401 просто читается `refresh_token` из `sessionStorage`, но реальный запрос `axios.post('/auth/refresh', ...)` закомментирован, поэтому вместо обновления токена происходит разлогин.
4. **Включить Vite proxy** в `vite.config.ts` для удобной локальной разработки — конфиг с проксированием `/api` и `/health` на `localhost:8000` уже написан, просто закомментирован целиком.
5. **Покрытие тестами** — на момент анализа тестов нет вообще (ни одного `*.test.*`/`*.spec.*` файла).
6. **Раскомментировать блоки в `Products.tsx`** (например загрузку себестоимости для каждого товара, `Products.tsx:52-67`) когда соответствующие данные будут доступны с бэка.
7. **Реализовать экспорт в PDF** в `analyticsApi.exportPdf` — сейчас метод сразу бросает `Error('Экспорт в PDF временно недоступен')`.
8. **Добавить type-checked linting** в ESLint — сейчас в `eslint.config.js` подключён `tseslint.configs.recommended` (без анализа типов), а не `tseslint.configs.recommendedTypeChecked`, для которого также потребуется `parserOptions.project`.
9. **Проверить типизацию всего проекта.** `npx tsc --noEmit -p tsconfig.app.json` на момент этой правки даёт ~77 ошибок в разных файлах (неиспользуемые импорты, несовпадения типов в `dashboardApi.ts`, `ProductDetail.tsx`, `Login.tsx` и др.) — `npm run build` сейчас не проходит. Требует отдельного прохода, не связанного с конкретной фичей.
